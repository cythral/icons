image: node:9

build:
  stage: build
  script:
    - npm install
    - npm run build
    - npm run pages
  artifacts:
    expire_in: 1 week
    paths:
      - public

pages:
  stage: deploy
  script:
  - echo 'hi'
  artifacts:
    paths:
      - public
  only:
  - master