image: node:9

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    expire_in: 1 week
    paths:
      - public

pages:
  stage: deploy
  script:
  - echo 'echo'
  artifacts:
    paths:
      - public
  only:
  - master